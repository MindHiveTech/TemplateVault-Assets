[project]
name = "templatevault-assets"
version = "1.0.0"
description = "Public GitHub release hosting for TemplateVault workflows"
requires-python = ">=3.9"

# No dependencies - this repository uses GitHub Actions bash scripts only
